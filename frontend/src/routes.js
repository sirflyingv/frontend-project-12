const baseUrl = '';
const apiUrl = 'api/v1';

export const dataUrl = [baseUrl, apiUrl, 'data'].join('/');
export const loginUrl = [baseUrl, apiUrl, 'login'].join('/');
export const signUpUrl = [baseUrl, apiUrl, 'signup'].join('/');
